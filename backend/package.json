{
  "name": "aftermeta-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --tsconfig tsconfig.json src/api/server.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node --enable-source-maps dist/api/server.js",
    "migrate": "tsx --tsconfig tsconfig.json src/scripts/migrate.ts",
    "test": "tsx --tsconfig tsconfig.json scripts/run-tests.ts"
  },
  "dependencies": {
    "better-sqlite3": "^9.6.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsx": "^4.19.2"
  }
}
